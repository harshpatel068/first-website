const runtimeConfig = require('./dist/shared/lib/runtime-config');

module.exports = {
  ...runtimeConfig, // Maintain your existing runtime configuration if needed
  trailingSlash: true, // Ensures directories are treated as trailing slashes and generates index.html
  basePath: '', // Make sure basePath is empty for GitHub Pages
  assetPrefix: '', // Ensure assetPrefix is empty for static assets
  exportPathMap: async function (
    defaultPathMap,
    { dev, dir, outDir, distDir, buildId }
  ) {
    return {
      '/': { page: '/' },
      // Add other paths here as needed, for example:
      // '/about': { page: '/about' },
    };
  },
};
